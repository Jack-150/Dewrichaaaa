# Birthday Website 🎂
This package already contains the 4 uploaded photos and uploaded COZI MP3.
Upload the contents to GitHub and enable GitHub Pages from `main` / root.
There is no play button. The page requests autoplay + loop; audible autoplay can still be blocked by a browser until the visitor interacts with the page.